"use client";

import { PromptFormWrapper } from "@/components";

const CreatePromptPage = () => {
  return <PromptFormWrapper type="create" onFormSubmit={() => undefined} />;
};

export default CreatePromptPage;
